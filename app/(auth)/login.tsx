import { Text } from "react-native";
import { Screen } from "@core/components";

export default function LoginScreen() {
  return (
    <Screen>
      <Text>Login — Phase 3</Text>
    </Screen>
  );
}
