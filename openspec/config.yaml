# openspec/config.yaml
# Auto-generated by sdd-init — do not hand-edit the context section
schema: spec-driven

context: |
  Tech stack: Expo (React Native 0.81.5) + TypeScript (strict) + Supabase + Zustand + TanStack Query + NativeWind (Tailwind)
  Architecture: Feature modules (src/features/{auth,habits,pomodoro,settings}) + Expo Router (app/) + shared primitives (src/shared/)
  Testing: No test runner configured. strict_tdd: false.
  Style: Named exports for components (export function), interfaces for props, Tailwind className styling, direct imports (no barrels), Zustand stores with persist middleware

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    - Use named exports (export function) for components
    - Import directly from each file — no barrel exports
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npm run web"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
